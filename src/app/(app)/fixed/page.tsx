import CategoryGroupView from "@/components/CategoryGroupView";

export default function FixedPage() {
  return (
    <CategoryGroupView
      group="fixed"
      title="Fixed charges"
      subtitle="Rent, bills, subscriptions — the costs that repeat every month."
    />
  );
}
