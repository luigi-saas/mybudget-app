import CategoryGroupView from "@/components/CategoryGroupView";

export default function VariablePage() {
  return (
    <CategoryGroupView
      group="variable"
      title="Variable spending"
      subtitle="Groceries, fuel, family, going out — the costs that change month to month."
    />
  );
}
